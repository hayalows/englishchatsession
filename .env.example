# The public student finder still works without environment variables.
# Set this only to enable the password-protected /admin console.
ADMIN_PASSWORD=replace-with-a-long-random-value

# Server-only database connection for anonymous first-party analytics.
# Keep the real value in Vercel environment variables and out of GitHub.
DATABASE_URL=
